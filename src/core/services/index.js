
///
const api = `${process.env.PUBLIC_URL}/api`;

export const surveys = `${api}/surveys.json`;

export const login = `${api}/login.json`;

export const Logout = `${api}/logout.json`;

export const dashboard = `${api}/dashboard.json`;
